# Locked reference outputs

These CSVs are frozen manuscript-facing analysis artifacts. They are included so a reviewer can independently check the reported seed counts, summary statistics, paired tests, practical CPCB results, external-rank results, and LOCO summary.

Do not overwrite these files when rerunning the pipeline. Fresh runs are written to `outputs/`.
